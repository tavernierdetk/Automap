# Scene brief — lakeshore (engine test fixture)

**Status: legacy E1/SCD-era proof scene, restored 2026-07-18** after the
clean-slate curation removed it while tools/ci.sh (test_baked_scene,
test_full_loop) still bakes and walks it. It files under `originals/`
as a FIXTURE: its acceptance read is "the engine test suite passes",
not scene craft. Not part of the vaporis world; never gets exits into
it. Retire it only together with the tests that walk it.
